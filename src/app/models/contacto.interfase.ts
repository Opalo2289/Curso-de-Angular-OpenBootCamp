/*
* Para el ejemplo se crea una carpeta dentro de app que se llama mocks para simular los dartos,
desde alli luego se exporta
*/
export interface IContacto {
   id: number;
   nombre: string;
   apellidos: string;
   email: string;
   edad: number; 
}